import { createTheme } from "@mui/material/styles";

const materialUITheme = createTheme({
  palette: {
    primary: {
      main: "#FF5959", // Couleur principale
    },
    secondary: {
      main: "#FFFFFF", // Couleur secondaire
    },
  },
});

export { materialUITheme };
